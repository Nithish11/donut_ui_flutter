import 'package:flutter/material.dart';

class pancakeTab extends StatelessWidget {
  const pancakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("PANCAKE TAB")),
    );
  }
}
